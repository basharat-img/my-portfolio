# MongoDB Atlas connection configuration
MONGODB_URI=
MONGODB_DB_NAME=
# Optional: override the default admins collection name
# MONGODB_ADMINS_COLLECTION=admins

# JWT token signing secret for admin authentication
JWT_SECRET=
